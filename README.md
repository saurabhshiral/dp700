# DP-700 Exam Prep

Single-page Microsoft DP-700 / Microsoft Fabric exam prep app.

## Use

Open `index.html` directly, or publish the repository with GitHub Pages.

## Update

Replace `index.html` with the latest exported app and commit to `main`.
The included GitHub Pages workflow publishes the site automatically.
